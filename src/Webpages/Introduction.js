import React from "react";

const Introduction = (props) => {
  return (
    <div>
      <p>
        Welcome, {props.name}! Shall we start finding the playlist that fits your
        mood today?
      </p>
    </div>
    <div>
        <button onClick={}>Continue</button>
    </div>
  );
};

export default Introduction;
